name: suaams
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  flutter_secure_storage: ^9.0.0
  http: ^1.2.0 # or dio for advanced interceptors
  nfc_manager: ^3.3.0 # For NFC interactions (if extending beyond HCE)
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
